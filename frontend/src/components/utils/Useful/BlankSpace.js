import React from 'react';

function BlankSpace() {
  return (
    <div className='blank-space' style={{ height: `var(--baseline-2x)`, borderLeft: `var(--border)` }}>
      {/* You can add any additional content or styling within the Box */}
    </div>
  );
}

export default BlankSpace;
