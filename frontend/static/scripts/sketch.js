let car;
let road;
const width = 200;
function setup() {
    createCanvas(200, windowHeight);
    // road = new Road(200 / 2, 200 * 0.9);
    // car = new Car(road.getLaneCentre(2), 100, 30, 50);
}

function draw() {
    background(212, 212, 212);
    // car.update();
    // translate(0, -car.y + windowHeight * .7);

    // road.draw();

    // car.draw();

}

// function keyPressed() {
//     car.control.keyPressed();
// }


// function keyReleased() {
//     car.control.keyReleased();
// }